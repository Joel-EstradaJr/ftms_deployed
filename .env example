#! FRONTEND (Next.js) PORT CONFIGURATION
# Next.js will run on port 3000
PORT=3000

# Frontend base URL
NEXT_PUBLIC_BASE_URL=http://localhost:3000

# Note: This is a UI-only frontend - no API backend required for now
# NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
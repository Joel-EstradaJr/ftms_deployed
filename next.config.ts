import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Note: ESLint configuration has been removed as it's no longer supported in Next.js 16+
  // Use `pnpm run lint` separately to check for linting issues
};

export default nextConfig;
